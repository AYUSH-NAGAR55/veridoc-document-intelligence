fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0

# Document processing
# Version pins below are deliberately loose (">=") rather than exact ("==") because
# pymupdf/opencv/faiss are native-compiled packages - an exact pin can force pip to
# build from source (requiring a C++ compiler) on a Python version newer than the
# pin's prebuilt wheels. ">=" lets pip pick the latest wheel that matches your
# Python version instead.
pymupdf>=1.24.13
pdfplumber>=0.11.4
python-docx>=1.1.2
pandas>=2.2.2
openpyxl>=3.1.5
Pillow>=10.4.0
pytesseract>=0.3.13
opencv-python-headless>=4.10.0.84

# AI / RAG
sentence-transformers>=3.1.1
faiss-cpu>=1.8.0
httpx>=0.27.2
numpy>=1.26,<2.0

# Optional production DB drivers (SQLite works with no extra driver)
psycopg2-binary>=2.9.9
pymysql>=1.1.1
